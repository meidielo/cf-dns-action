{
  "name": "cf-dns-action",
  "version": "1.0.0",
  "private": false,
  "description": "GitHub Action that registers custom domains in Netlify and creates/updates matching Cloudflare CNAME records. Idempotent, scoped-token friendly.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "all": "npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "keywords": [
    "github-action",
    "cloudflare",
    "netlify",
    "dns",
    "cname",
    "custom-domain",
    "idempotent"
  ],
  "author": "Meidie Fei",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@actions/core": "^2.0.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^24.0.0",
    "@vercel/ncc": "^0.38.1",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "typescript": "^5.5.4",
    "typescript-eslint": "^8.18.0",
    "vitest": "^4.1.4"
  }
}
